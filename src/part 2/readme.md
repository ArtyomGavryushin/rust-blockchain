# That's part include's blockchain with DB and Mini-client service

`cargo run addblock "any string"` - command for adding block in blockchain 
`cargo run printchain` - command for outputting data from blockchain db 
